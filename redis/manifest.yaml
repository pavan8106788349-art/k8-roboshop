apiVersion: apps/v1
kind: Deployment

metadata:
  name: redis
  namespace: roboshop
  labels:
    project: roboshop
    tier: db
    component: redis

spec:
  replicas: 1

  selector:
    matchLabels:
      project: roboshop
      tier: db
      component: redis

  template:
    metadata:
      labels:
        project: roboshop
        tier: db
        component: redis

    spec:
      containers:
        - name: redis
          image: redis:7.0
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: roboshop
  labels:
   project: roboshop
   tier: db
   component: redis
spec:
  selector:
    # labels are used as selectors
    project: roboshop
    tier: db
    component: redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379